import React from "react";
import { Expandable as BaseExpandable, ExpandableProps } from "../../src";

const Expandable = (props: ExpandableProps) => <BaseExpandable {...props} />;

export default Expandable;
