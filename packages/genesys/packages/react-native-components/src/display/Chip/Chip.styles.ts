import styled from "@peersyst/react-native-styled";
import { Row } from "../../layout/Row";
import { Text } from "react-native";

export const ChipRoot = styled(Row, { alignItems: "center" })(() => ({}));

export const ChipLabelText = styled(Text)(() => ({}));
