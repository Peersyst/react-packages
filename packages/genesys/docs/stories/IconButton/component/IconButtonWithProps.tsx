import { IconButton, IconButtonProps } from "@peersyst/react-components";

const IconButtonWithProps = (props: IconButtonProps): JSX.Element => <IconButton {...props} />;

export default IconButtonWithProps;
