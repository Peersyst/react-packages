import { Canvas, Meta } from "@storybook/addon-docs";
import { Button, Row } from "@peersyst/react-components";
import themedContainer from "../styles/themedContainer";

<Meta
    title="Components/Inputs/Button"
    component={Button}
    parameters={{
        docs: {
            container: themedContainer,
        },
    }}
/>

# Button

A regular button with some extra functionalities and default styles.

## Usage

Usage is the same as a normal button:

```jsx
<Button>Click me</Button>
```

<Canvas>
    <Row justifyContent="center">
        <Button>Click me</Button>
    </Row>
</Canvas>

### Disabled

```jsx
<Button>I'm disabled️</Button>
```

<Canvas>
    <Row justifyContent="center">
        <Button disabled>I'm disabled</Button>
    </Row>
</Canvas>

### Loading

```jsx
<Button loading>Click me</Button>
```

<Canvas>
    <Row justifyContent="center">
        <Button loading>Click me</Button>
    </Row>
</Canvas>

### Custom loading

```jsx
<Button loading loadingElement={"Loading..."}>
    Click me
</Button>
```

<Canvas>
    <Row justifyContent="center">
        <Button loading loadingElement={"Loading..."}>
            Click me
        </Button>
    </Row>
</Canvas>

### Size

```jsx
<Row justifyContent="center" alignItems="center" gap={20}>
    <Button size="sm">Small</Button>
    <Button size="md">Medium</Button>
    <Button size="lg">large</Button>
</Row>
```

<Canvas>
    <Row justifyContent="center" alignItems="center" gap={20}>
        <Button size="sm">Small</Button>
        <Button size="md">Medium</Button>
        <Button size="lg">large</Button>
    </Row>
</Canvas>

### Variants

```jsx
<Row justifyContent="center" alignItems="center" gap={20}>
    <Button variant="filled">Filled</Button>
    <Button variant="outlined">Outlined</Button>
    <Button variant="text">Text</Button>
</Row>
```

<Canvas>
    <Row justifyContent="center" alignItems="center" gap={20}>
        <Button variant="filled">Filled</Button>
        <Button variant="outlined">Outlined</Button>
        <Button variant="text">Text</Button>
    </Row>
</Canvas>
