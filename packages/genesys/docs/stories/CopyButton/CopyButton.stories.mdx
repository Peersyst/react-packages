import { Canvas, Meta } from "@storybook/addon-docs";
import {CopyButton, Row, Typography} from "@peersyst/react-components";
import themedContainer from "../styles/themedContainer";

<Meta
    title="Components/Utils/CopyButton"
    component={CopyButton}
    parameters={{
        docs: {
            container: themedContainer,
        },
    }}
/>

# CopyButton

Button used to copy text.

<Canvas>
    <Row justifyContent="center" alignItems="center" gap={10}>
        <Typography variant="body1">Copies "HELLO"</Typography>
        <CopyButton text="HELLO" />
    </Row>
</Canvas>

## Usage

````jsx
<Row justifyContent="center" alignItems="center" gap={10}>
    <Typography variant="body1">Copies "HELLO"</Typography>
    <CopyButton text="HELLO" />
</Row>
````
