import { Canvas, Meta } from "@storybook/addon-docs";
import { Slider } from "@peersyst/react-components";
import themedContainer from "../styles/themedContainer";
import ControlledSlider from "./component/ControlledSlider";

<Meta
    title="Components/Inputs/Slider"
    component={Slider}
    parameters={{
        docs: {
            container: themedContainer,
        },
    }}
/>

# Slider

Sliders are used to obtain one value from a range.

## Example

<Canvas>
    <Slider min={0} max={100} defaultValue={[0, 50]} disableSwap valueLabelDisplay="on" label="Title" />
</Canvas>

## Usage

Its usage can be controlled or uncontrolled:

### Uncontrolled usage

```jsx
<Slider min={0} max={100} />
```

<Canvas>
    <Slider min={0} max={100} size="sm" />
</Canvas>

### Controlled usage

<Canvas>
    <ControlledSlider />
</Canvas>

## Styling

Color styling is used via the Slider's color css attribute. If a higher level of customization is needed, there are className and style props for every component. However, to customize the thumb, you have to style the inner input's one (`input::-webkit-slider-thumb` and `input&::-moz-range-thumb`)
