import { ErrorBoundary, Button } from "@peersyst/react-components";
import { Canvas, Meta } from "@storybook/addon-docs";
import themedContainer from "../styles/themedContainer";

<Meta
    title="Components/Utils/ErrorBoundary"
    component={ErrorBoundary}
    parameters={{
        docs: {
            container: themedContainer,
        },
    }}
/>

# ErrorBoundary

This component caches all errors thrown by its children.

Then, it can provide an onError action.

## Usage

```jsx
<ErrorBoundary onError={handleError}>{children}</ErrorBoundary>
```
