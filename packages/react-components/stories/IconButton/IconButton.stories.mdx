import {Canvas, Meta} from "@storybook/addon-docs";
import themedContainer from "../styles/themedContainer";
import {SuccessIcon} from "../../src/assets/icons";
import {Row, IconButton} from "../../src";

<Meta
    title="Components/Icons/IconButton"
    component={IconButton}
    parameters={
        {
            docs: {
                container: themedContainer
            },
        }
    }
/>

# IconButton

Component used to treat an Icon as a Button.

## Usage

````jsx
<IconButton
    style={{ fontSize: "24px" }}
    onClick={handleClick}
>
    <SuccessIcon />
</IconButton>
````

<Canvas>
    <Row justifyContent="center" className="with-icon">
        <IconButton Icon={SuccessIcon} className="icon" style={{ fontSize: "24px" }}>
            <SuccessIcon />
        </IconButton>
    </Row>
</Canvas>>
