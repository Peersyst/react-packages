import styled from "@peersyst/react-native-styled";
import { Typography } from "../../display/Typography";

export const FormControlHintRoot = styled(Typography, { light: true })(() => ({
    marginLeft: 5,
}));
